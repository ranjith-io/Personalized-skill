// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int num ,count,digit;
    scanf("%d",&digit);
    num=digit;
    while(num!=0){
    num=num/10;
    count++;
        }
    printf("%d",count);
    
    while (digit!=0)
    {
        int last = digit%10;
        int first = digit 
    }
    
    
    
}